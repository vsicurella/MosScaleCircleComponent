{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Demo (MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "externalConsole": false,
            "preLaunchTask": "CMake: build demo"
        }
    ]
}
